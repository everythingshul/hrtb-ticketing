services:
  - type: web
    name: hrtb-ticketing
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
